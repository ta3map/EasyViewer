# TO_DO

## Задачи

### 0. Починить восстановление положения окна из настроек
- ⏳ Требуется исправление
- **Проблема**: При перемещении или изменении размера окон GUI их новое положение не сохраняется в настройки
- **Затронутые файлы**:
  - `signalViewerGUI.m` (строка 244-247): функция `saveSettings()` намеренно не сохраняет `figure_position` (комментарий "Не сохраняем положение окна")
  - `signalAnalysisGUI.m` (строка 4519): функция `closeSignalAnalysisWindow()` не сохраняет положение окна
- **Текущее поведение**:
  - Положение окна загружается из `ev_settings.mat` через `loadGlobalSettings()` при запуске
  - При закрытии/изменении размера окна новое положение не сохраняется обратно в файл настроек
  - При следующем запуске используется старое сохраненное положение или базовое из JSON
- **Требуемое исправление**:
  - Добавить сохранение `figure_position` в `saveSettings()` в `signalViewerGUI.m`
  - Добавить сохранение `figure_position` в `closeSignalAnalysisWindow()` в `signalAnalysisGUI.m`
  - Сохранять текущее положение окна (`f.Position` или `signalFig.Position`) в глобальную переменную `figure_position` перед сохранением в файл


### 1. Функция построения боксплотов из плоской таблицы
- ✅ Реализовано: `functions/boxplotFromTableGUI.m`
- Загрузка данных из MAT файлов (flatTable) и Excel файлов
- Фильтрация данных через MATLAB формулы в формате: `A: условие1; B: условие2`
- Построение боксплотов с точками данных
- Опциональные статистические тесты (ttest2) со скобками значимости
- Кастомные подписи и цвета групп
- Экспорт графиков (PNG, PDF, FIG, EPS)

### 2. Функция построения зависимостей из плоской таблицы
- ⏳ В ожидании реализации
- Загрузка плоской таблицы из MAT файла
- Интерфейс выбора столбцов X и Y
- Редактор формул MATLAB для фильтрации данных
- Поддержка типов графиков:
  - Scatter plot (зависимость Y от X)
  - Корреляционные графики (матрица корреляций для нескольких переменных)
  - Регрессионные графики с линией тренда
- Настройка параметров визуализации

### 3. Мета-анализ: выбор структуры при нескольких типах мета-файлов
- ⏳ В ожидании реализации
- Модифицировать `functions/metadataAnalysis.m`
- При обнаружении нескольких типов структур в мета-файлах показывать диалог выбора
- Определение типов структур на основе различий в полях между файлами
- Диалог `listdlg` с описанием каждой структуры
- Сохранение выбранной структуры для дальнейшей обработки

### 4. Просмотр OIS сигналов
- ⏳ В ожидании реализации
- Создать функцию `functions/viewOISSignalsGUI.m`
- Загрузка OIS (Optical Imaging Signals) данных
- Определение формата файлов OIS (возможно, расширение `.ois` или специальная структура в MAT файлах)
- Визуализация оптических сигналов с поддержкой:
  - Многоканального отображения
  - Временной шкалы
  - Настройки цветовой карты для оптических данных
- Интеграция с существующей системой просмотра сигналов

### 5. Дополнительные функции анализа

#### 5.1. Статистический анализ из плоской таблицы
- ⏳ В ожидании реализации
- Создать `functions/statisticsFromTableGUI.m`
- Выбор групп для сравнения через формулы фильтрации
- Выполнение статистических тестов (t-test, ANOVA, Mann-Whitney и т.д.)
- Отображение результатов в таблице

#### 5.2. Построение гистограмм из плоской таблицы
- ⏳ В ожидании реализации
- Создать `functions/histogramFromTableGUI.m`
- Выбор столбца для построения гистограммы
- Фильтрация через формулы
- Настройка количества бинов и нормализации

#### 5.3. Экспорт графиков
- ✅ Частично реализовано (в boxplotFromTableGUI)
- Экспорт в PNG, PDF, SVG, FIG
- Настройка разрешения и размеров
- Сохранение данных графика в MAT файл
- Добавить экспорт во все GUI функции визуализации

