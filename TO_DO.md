# TO_DO

## Задачи

упростить извлечение параметров в autoclusterPermutationTest
сделать чтобы еще при загрузке в file_manager все было готово для модуля


### 1. Функция построения зависимостей из плоской таблицы
- ⏳ В ожидании реализации
- Загрузка плоской таблицы из MAT файла
- Интерфейс выбора столбцов X и Y
- Редактор формул MATLAB для фильтрации данных
- Поддержка типов графиков:
  - Scatter plot (зависимость Y от X)
  - Корреляционные графики (матрица корреляций для нескольких переменных)
  - Регрессионные графики с линией тренда
- Настройка параметров визуализации

### 2. Мета-анализ: выбор структуры при нескольких типах мета-файлов
- ⏳ В ожидании реализации
- Модифицировать `functions/metadataAnalysis.m`
- При обнаружении нескольких типов структур в мета-файлах показывать диалог выбора
- Определение типов структур на основе различий в полях между файлами
- Диалог `listdlg` с описанием каждой структуры
- Сохранение выбранной структуры для дальнейшей обработки

### 3. Просмотр OIS сигналов
- ⏳ В ожидании реализации
- Создать функцию `functions/viewOISSignalsGUI.m`
- Загрузка OIS (Optical Imaging Signals) данных
- Определение формата файлов OIS (возможно, расширение `.ois` или специальная структура в MAT файлах)
- Визуализация оптических сигналов с поддержкой:
  - Многоканального отображения
  - Временной шкалы
  - Настройки цветовой карты для оптических данных
- Интеграция с существующей системой просмотра сигналов

### 4. Дополнительные функции анализа

#### 4.1. Статистический анализ из плоской таблицы
- ⏳ В ожидании реализации
- Создать `functions/statisticsFromTableGUI.m`
- Выбор групп для сравнения через формулы фильтрации
- Выполнение статистических тестов (t-test, ANOVA, Mann-Whitney и т.д.)
- Отображение результатов в таблице

#### 4.2. Построение гистограмм из плоской таблицы
- ⏳ В ожидании реализации
- Создать `functions/histogramFromTableGUI.m`
- Выбор столбца для построения гистограммы
- Фильтрация через формулы
- Настройка количества бинов и нормализации

### 5. Удаление ненужных связей
- ⏳ В ожидании реализации
- Проанализировать отчет `Dependency Analyzer Report.html`
- Определить ненужные зависимости между модулями
- Удалить неиспользуемые связи для упрощения архитектуры кодовой базы

### 6. Отмена прогресс-баров
- ⏳ В ожидании реализации
- Разобраться с механизмом отмены операций с прогресс-барами
- Реализовать возможность прерывания длительных операций через интерфейс прогресс-бара

### 7. Разобраться с причиной дубликатов при мета-анализе
- ⏳ В ожидании реализации
- Исследовать причину появления дубликатов записей при выполнении мета-анализа
- Проанализировать функцию `functions/metadataAnalysis.m`
- Определить источник дублирования данных
- Реализовать устранение дубликатов или предотвращение их появления

### 8. Перевести File Manager и Boxplot From Table на относительные масштабируемые координаты
- ⏳ В ожидании реализации
- Модифицировать `functions/fileManagerGUI.m` и `functions/boxplotFromTableGUI.m`
- Реализовать систему относительных координат как в `signalViewerGUI.m` и `signalAnalysisGUI.m`:
  - Загрузка координат из JSON файла
  - Преобразование относительных координат в абсолютные на основе `base_figure_position`
  - Переиспользовать аналогичным образом функцию `resizeComponents` для автоматического масштабирования при изменении размера окна
  - Переиспользование аналогичным образом функции `getElementPosition` для получения координат элементов
- Добавить разворот окна после установки координат (`f.WindowState = 'maximized'`)
- Переместить файлы координат для всех таких автомасштабируемых окон (signalViewerGUI signalAnalysisGUI и новых) в папку `configs/window_coords`:
  - `boxplotFromTableGUI_coords.json` → `configs/window_coords/boxplotFromTableGUI_coords.json`
  - Создать `configs/window_coords/fileManagerGUI_coords.json` для File Manager
- Обновить пути к файлам координат в коде

