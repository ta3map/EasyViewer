function drawPlotScaleBars(ax, ampValue, timeSpan, selectedUnit, xlimsOverride, ylimsOverride)
%DRAWPLOTSCALEBARS Thick Y/X scale bars in axes coords; labels from ampValue/timeSpan.

updatePlotScaleBars(ax, gobjects(0), ampValue, timeSpan, selectedUnit, xlimsOverride, ylimsOverride);

end
